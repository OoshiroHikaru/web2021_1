<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>Song List</title>
</head>
<body>
  <h2>曲名一覧</h2>
  <table border="1">
    <tr>
      <th>ID</th>
      <th>曲名</th>
    </tr>
    <% data.forEach(function(row) { %>
      <tr>
        <td><%= row.id %></td>
        <td><%= row.song_name %></td>
      </tr>
    <% }); %>
  </table>
</body>
</html>

