<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>曲 修正</title>
    <style>
        body {
            background-color: #000000;
            color: #ffffff;
        }
        a:link, a:visited {
            color: #ff0000;
        }
    </style>
</head>
<body>
    <h2>曲 修正</h2>
    <form action="/edit" method="post">
        <input type="hidden" name="song_id" value="<%= song.id %>"> <!-- 曲IDを隠しフィールドに追加 -->
        <h5>曲名</h5>
        <input type="text" name="name" value="<%= song.name %>"><br>
        <h5>アーティストID</h5>
        <input type="number" name="artist_id" value="<%= song.artist_id %>"><br>
        <input type="submit" value="修正">
    </form>
</body>
</html>

